# 이전 프로젝트(크로스보더 협업 중재 서비스)에서 이식한 지식 자산

> 출처: `C:\middle_hakerton` (2026-08-04 ~ 08-12, 86개 태스크 완료, 프로덕션 배포까지 검증).
> **이 문서는 참고 자료다. `docs/Spec.md`와 충돌하면 무조건 Spec이 이긴다.**

## 금지 — 이식하면 안 되는 구 결정 (Spec이 반대로 확정함)

| 구 프로젝트 결정 | Sai에서는 |
|---|---|
| 발송 내역·diff 원문을 DB에 저장 | **Zero Retention** — 본문은 서버·DB·로그 어디에도 저장 금지, 카운트·메타데이터만 (필수 5) |
| 긴급도는 AI 판정 후 override만 | **사용자 사전 선택 가능**, 미선택 시 AI 1차 판정 (필수 1) |
| 예약 발송은 제안만 | Low 모드 퇴근 시간대 **강제 예약** (필수 6) |
| 이모지는 경고만, 자동 변경 금지 | 위험 이모지 **자동 교체 후 안내** (권장 4) |
| 공개 프로필 URL 1건만, OAuth 금지 | **GitHub OAuth 2.0** 행동 데이터 연동 (audit 3) |

## 그대로 쓸 수 있는 자산

1. **프롬프트 5종** (`reference/c1.ts`~`c7.ts`) — 라이브 LLM으로 검증 완료. TS 파일이지만 프롬프트 본문은 문자열이라 언어 무관.
   - `c1.ts` 긴급도 분류(CRITICAL/NORMAL/LOW + 근거) → 필수 1
   - `c2.ts` 톤 변환 — **긴급도 보존 필터, 쿠션어 과잉 사과 억제, 한국어 어미 긴급도 복원, 날짜·숫자 비모호 정규화, 존댓말 레벨 혼용 감지가 이미 들어 있음**. Spec의 [권장 1] 긴급도 쉴드·[권장 13] 통화/단위 병기를 프롬프트 단계에서 상당 부분 해결한다.
   - `c4.ts` 역번역 → 필수 3
   - `c6.ts` 하소연→티켓 4섹션(+우려 수준) → 필수 4 (Spec은 3단이므로 [우려 수준]은 메타로만)
   - `c7.ts` 결정사항 요약 → 부가 7
2. **테스트 케이스 74건** (`reference/TestCases-legacy.md`) — 긴급도 판정·핵심 정보 보존·역번역·티켓 변환 입력/기대값. `/v1/refine` 통합 테스트의 시드로 재사용.
3. **C6 오탐 방지 규칙** — 감정 신호가 낮은 입력에는 티켓 변환을 제안하지 않는다(감지 임계 프롬프트에 반영됨). Spec의 "감지 시 제안"과 정합.

## 실측으로 얻은 기술 교훈 (같은 함정을 다시 밟지 말 것)

| # | 교훈 | 실측 근거 |
|---|---|---|
| 1 | **jsdom 테스트는 확장 번들 크래시를 원리적으로 못 잡는다** — `process is not defined`로 콘텐츠 스크립트가 전 페이지 즉사했는데 vitest는 전부 green이었다. `vite.config`에 `define: { 'process.env': {} }` 계열 처리 + **실브라우저 unpacked 로드 스모크를 Day 1부터** | 구 T80, 2026-08-10 |
| 2 | **contentEditable에 `textContent` 대입은 조용히 실패하거나 서식을 파괴한다** — 리치 에디터(Gmail 등)는 Range/`execCommand('insertText')` 기반 삽입 필요. Gmail 답장에서 전체 본문 치환은 인용문을 날린다 | 구 T47/T49 스파이크 |
| 3 | **GitHub PR 페이지는 정적 CSS 선택자가 0매치** (React/Primer 동적 렌더) — DOM 의존 기능은 반드시 실페이지에서 선택자 검증 | 구 T29 QA 실측 |
| 4 | **수신자 DOM 자동 감지는 범용 규칙이 성립하지 않는다** — 사이트마다 마크업이 완전히 다름(Discourse 자체 클래스, Google Groups 난독화). Spec audit 2의 "수동 선택 드롭다운 백업"을 백업이 아니라 **주 경로**로 설계할 것 | 구 T66 스파이크, 2026-08-10 |
| 5 | **LLM 실패·크레딧 소진 폴백** — 사전 준비된 데모 응답으로 폴백하되 **"폴백 중" 표시를 화면에 명시**(실제 결과로 오인 금지). 시연 중 키 장애 대비의 생명줄 | 구 AC-029/AC-041, 프로덕션에서 실동작 확인 |
| 6 | **동일 입력 LLM 캐시** — 같은 입력 재호출을 캐시로 돌려주면 비용 절감 + 데모 재현성 확보. 단, "독립 재현 검증"이 필요할 때 캐시가 방해하므로 바이패스 플래그를 처음부터 | 구 llm_cache |
| 7 | **EU AI Act Art 5(1)(f)** — 직장에서 자연인의 감정을 추론하는 AI는 금지(2025-02 발효, 전세계 매출 7% 과징금). **B2B 마찰 리포트("하소연 감지 횟수"를 리더에게 노출)가 정면으로 걸린다.** Spec대로 v1은 목업 배너만 두는 것이 안전하며, 발표에서 EU 시장을 전제로 말하지 않는다 | 구 PRD Risk, measured 2026-08-04 |
| 8 | **개인 점수·국가 단정 회피는 심사에서도 방어력** — 서술형 태그(필수 9의 G1/G2)와 근거 인용("코멘트 평균 2문장 → 간결 제안")은 구 프로젝트에서 프라이버시 리스크를 크게 줄인 검증된 패턴 |
| 9 | **병렬 작업은 반드시 별도 워크트리/클론** — 같은 디렉토리에서 병렬 브랜치 작업 시 HEAD가 서로 바뀜 | 구 T47/T49에서 실제 발생 |
| 10 | **Vercel/호스팅 별칭은 배포 성공과 별개** — 프로덕션 도메인이 옛 배포를 가리키는 사고 2회. 배포 후 반드시 공개 URL을 curl로 실측 | 구 T36, 2026-08-12 |
| 13 | **Windows 셸에서 `curl -d '{"text":"한글..."}'`는 UTF-8을 깨뜨린다 — 깨진 입력을 모델 결함으로 오진했다.** 통합 점검 중 "마감(`내일까지`)이 교정문에서 사라지고, 원문에 없는 `merge`가 지어내지고, `급해요`인데 urgency가 NORMAL"로 나와 프롬프트 결함으로 판단할 뻔했다. 실제로는 **인라인 `-d`의 한글이 전송 과정에서 손상**돼 모델이 깨진 텍스트를 받은 것이었다. 같은 입력을 UTF-8 파일로 보내니(`--data-binary @body.json`) `by tomorrow` 보존 + `urgency: CRITICAL` + `preserved.kind: "deadline"`으로 **전부 정상**이었다. → ① 한글이 든 요청 본문은 **반드시 UTF-8 파일로** 보낸다(`python`으로 `json.dumps(..., ensure_ascii=False)` 후 `--data-binary @파일`) ② `preserved`의 `sourceText`에 깨진 문자가 보이면 **모델을 의심하기 전에 내 파이프라인을 의심한다**(진단 신호로 유용하다) ③ 터미널 출력의 한글 깨짐은 표시 문제일 수 있으니 파일로 읽어 재확인한다 | 2026-08-13, S27 통합 점검 중 실제 오진 |
| 12 | **`curl`이 200을 줘도 "사용자에게도 최신"이라는 뜻이 아니다 — 경로가 다르면 엣지 캐시도 다르다.** 대시보드 재배포 후 `curl`로 새 HTML·새 JS·옛 번들 404까지 전부 확인했는데도 사용자 브라우저에는 옛 디자인이 계속 떴다. 원인은 **첫 배포 때 나간 `Cache-Control: max-age=3600`** — 사용자 쪽 CDN 엣지가 옛 index.html과 옛 JS를 **세트로** 캐시하고 있어 옛 화면이 멀쩡히 렌더됐다. 내 요청은 별도 프록시(`X-Served-By: cache-…`)를 경유해 그 엣지를 아예 타지 않았으므로 **원리적으로 재현 불가**였다. → ① 정적 호스팅은 `index.html`을 **`no-cache`**, 해시 붙은 `/assets/**`만 `immutable` 장기 캐시로 명시할 것(`firebase.json` `hosting.headers`) ② 캐시 의심 시 **쿼리스트링(`?v=2`)으로 URL을 바꿔** 확인시킬 것 — 강제 새로고침·브라우저 재시작으로는 엣지 캐시가 안 뚫린다 ③ "내 curl로는 최신"을 "고쳐졌다"의 근거로 삼지 말 것 | 2026-08-13, S18 배포 후 실제 발생 |
| 11 | **검증용 dev 서버(`vite`/`npx vite`)와 실제 언팩 로드 확장이 같은 `dist/`를 공유하면 서로 덮어쓴다** — UI 검증하려고 별도로 띄운 dev 서버가 `dist/`에 "CRXJS DEV MODE" 스텁을 써 넣어, 사용자가 chrome://extensions에 로드해 둔 확장이 통째로 깨졌다(사이드패널 로딩 실패, 31개 콘솔 오류). **검증용 서버는 반드시 별도 outDir(또는 임시 디렉터리)로 띄우고, 확장의 `dist/`는 `npm run build`(프로덕션 빌드) 전용으로 남긴다.** 증상은 "번역이 갑자기 안 됨"처럼 기능 버그로 오인되기 쉬우니, 실브라우저에서 갑자기 여러 기능이 동시에 죽으면 dist 오염부터 의심 | 2026-08-13, S21 검증 중 실제 발생 |
| 14 | **깨진 한글이 보인다고 인코딩 버그가 아니다 — 정상 음절이면 모델이 그렇게 뱉은 것이다.** AI 판정 근거에 「깂조」(← 강조), 「마감기」(← 마감일)가 나와 인코딩 파손을 의심했다. 아니었다. 판별 기준: **인코딩이 깨지면 mojibake(`ë°°í¬`)나 `�`가 나오지, 다른 정상 한글 음절로는 절대 안 바뀐다.** 「깂」은 U+AE42(ㄱ+ㅣ+ㅄ), UTF-8 `EA B9 82` — 의도한 「강」 U+AC15 `EA B0 95`과 2바이트가 다르다(비트 플립도, 절단 산물도 아니다). 그 바이트가 나오려면 **보낸 쪽이 그 글자를 실제로 써야** 한다. 파이프라인도 전 구간 정상이었다(프로바이더 `response.json()` / 프록시는 `Buffer.concat` **후** 디코드 + `Buffer.byteLength` + `charset=utf-8` / 확장 `response.json()`). → ① 깨진 한글을 보면 **먼저 코드포인트를 찍어 본다** — 정상 음절이면 파이프라인 수사를 중단하고 모델을 본다(Lessons #13의 반대 방향 오진이다: 거기선 모델을 의심했는데 파이프라인이 범인이었고, 여기선 그 반대다) ② 근본 원인은 **개발용 대체 provider(Gemini)를 쓰고 있다는 것** — `.env.example`이 OpenAI를 Spec §6-3 제출 경로 provider로 명시한다 ③ **출력 품질은 단위 테스트로 원리적으로 안 잡힌다.** `npm run test:refine:live`(실 API 20건)가 그걸 잡으라고 있는데 **한 번도 실행되지 않았다** — 화면 동작 확인과 출력 내용 검증은 다른 일이다 | 2026-08-14, S24 UI 평가 중 사용자 발견 |
| 15 | **브라우저 패널이 화면에 표시돼 있지 않으면 `requestAnimationFrame`도 CSS 트랜지션도 진행되지 않는다 — 하네스가 「정상」이라고 거짓말한다.** FAB 확장이 안 된다는 제보를 재현하려다 두 번 오진할 뻔했다: ① `.sai-fab-wrap-open`을 붙여도 `grid-template-columns`가 `0px`로 남아 "CSS가 안 먹는다"고 판단할 뻔했으나, `transition`을 끄고 재니 `143.875px`로 정상이었다(트랜지션이 멈춰 있던 것뿐이다) ② 스크롤을 줘도 버튼 위치가 안 바뀌어 "위치 추적이 원래 안 된다"고 볼 뻔했으나, 위치 갱신이 `rAF` 안에 있어 **콜백 자체가 실행되지 않은** 것이었다(인라인 `style.top`도 그대로였다). → ① 하네스에서 **시간에 의존하는 것**(트랜지션·애니메이션·rAF·`IntersectionObserver`)을 잴 때는 그 환경이 실제로 프레임을 그리는지 먼저 확인한다 ② 트랜지션이 걸린 속성은 `transition: none`으로 끄고 **목표값**을 잰다 ③ 잴 수 없으면 "확인했다"고 하지 말고 **미검증이라고 적는다** — 이 항목이 그래서 남았다 | 2026-08-14, S25 FAB 확장 조사 중 |
| 16 | **저장소 키 하나에 화면 상태 하나 — 같은 키를 읽는 state를 새로 만들면 토글이 한쪽만 갱신한다.** 밈 해설(S19)을 붙이면서 `RefinePopup`에 `CASUAL_TONE`을 읽는 `casualTone` state를 새로 만들었다. 그런데 헤더의 「캐주얼」 토글은 **이미 있던** `casualOn`만 갱신하고 저장소에 쓴다. 내 state는 마운트 때 읽은 값에 멈춰 있어, **캐주얼을 켜도 밈 탐지 목록이 안 넓어졌다**(사용자 스크린샷: 켠 상태인데 `no worries if not`이 안 잡히고 "2개"로 표시). 단위 테스트는 코어 함수만 보므로 321/321 green이었고 빌드도 통과했다 — **화면 배선 버그는 원리적으로 안 잡힌다.** → ① 저장소 키를 읽기 전에 **같은 컴포넌트에 이미 그 키를 읽는 state가 있는지 grep**한다(`grep -c "getLocal(STORAGE_KEYS.X"` 가 1이어야 한다) ② 기존 state를 재사용하면 선언 순서 때문에 TDZ가 나기 쉽다 — 그때는 **참조하는 쪽을 내리는 게 아니라 선언을 올린다**(토글 함수는 호출 시점이 늦어 그대로 둬도 된다) ③ 이 계열 버그는 사용자 스크린샷 한 장으로 드러난다. 화면을 바꾸는 작업은 테스트 green을 done의 근거로 쓰지 않는다 | 2026-08-14, S19 밈 해설에서 실제 발생 |
| 17 | **"크롬 확장"이라고 크롬에서 돌아가는 게 아니다 — 사용자가 엣지를 쓰고 있었고, `chrome.identity.getAuthToken`은 엣지에 없다.** 캘린더 연동이 계속 실패해서 OAuth 범위 미등록 → 콘솔 계정 불일치 → 게시 상태(프로덕션 vs 테스트)를 차례로 의심하며 사용자를 세 번 왔다 갔다 하게 했다. 실제 원인은 엣지가 뱉은 한 줄이었다: `This API is not supported on Microsoft Edge.` 이 API는 브라우저의 구글 계정 로그인 상태에 의존해서 엣지가 원리적으로 대응할 수 없다(`launchWebAuthFlow`는 양쪽 다 된다). → ① **연동·권한 작업을 시작하기 전에 "어느 브라우저에서 로드하셨나요"를 먼저 묻는다.** 스크린샷의 UI 크롬(chrome)만 봐도 구분되는데 확인하지 않았다 ② MV3 API를 쓸 때 **크롬 전용인지 확인한다** — `identity.getAuthToken`·`declarativeNetRequest` 일부·`sidePanel` 초기 버전 등이 갈린다 ③ **오류 원문을 화면에 드러내는 것이 진단을 몇 시간 앞당긴다.** 처음엔 실패를 전부 "거부"로 뭉뚱그려 놨는데, 크롬/엣지가 준 원문을 그대로 보여주도록 고치자마자 한 번에 끝났다 ④ manifest에 `key`를 박아 확장 ID를 고정해 둔 덕에 **브라우저를 바꿔도 OAuth 클라이언트를 다시 만들 필요가 없었다** — 이건 결과적으로 잘한 결정이었다 | 2026-08-14, S23 캘린더 연동 |
