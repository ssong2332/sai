# 이전 프로젝트(크로스보더 협업 중재 서비스)에서 이식한 지식 자산

> 출처: `C:\middle_hakerton` (2026-08-04 ~ 08-12, 86개 태스크 완료, 프로덕션 배포까지 검증).
> **이 문서는 참고 자료다. `docs/Spec.md`와 충돌하면 무조건 Spec이 이긴다.**

## 금지 — 이식하면 안 되는 구 결정 (Spec이 반대로 확정함)

| 구 프로젝트 결정 | Sai에서는 |
|---|---|
| 발송 내역·diff 원문을 DB에 저장 | **Zero Retention** — 본문은 서버·DB·로그 어디에도 저장 금지, 카운트·메타데이터만 (필수 5) |
| 긴급도는 AI 판정 후 override만 | **사용자 사전 선택 가능**, 미선택 시 AI 1차 판정 (필수 1) |
| 예약 발송은 제안만 | Low 모드 퇴근 시간대 **강제 예약** (필수 6) |
| 이모지는 경고만, 자동 변경 금지 | 위험 이모지 **자동 교체 후 안내** (권장 4) |
| 공개 프로필 URL 1건만, OAuth 금지 | **GitHub OAuth 2.0** 행동 데이터 연동 (audit 3) |

## 그대로 쓸 수 있는 자산

1. **프롬프트 5종** (`reference/c1.ts`~`c7.ts`) — 라이브 LLM으로 검증 완료. TS 파일이지만 프롬프트 본문은 문자열이라 언어 무관.
   - `c1.ts` 긴급도 분류(CRITICAL/NORMAL/LOW + 근거) → 필수 1
   - `c2.ts` 톤 변환 — **긴급도 보존 필터, 쿠션어 과잉 사과 억제, 한국어 어미 긴급도 복원, 날짜·숫자 비모호 정규화, 존댓말 레벨 혼용 감지가 이미 들어 있음**. Spec의 [권장 1] 긴급도 쉴드·[권장 13] 통화/단위 병기를 프롬프트 단계에서 상당 부분 해결한다.
   - `c4.ts` 역번역 → 필수 3
   - `c6.ts` 하소연→티켓 4섹션(+우려 수준) → 필수 4 (Spec은 3단이므로 [우려 수준]은 메타로만)
   - `c7.ts` 결정사항 요약 → 부가 7
2. **테스트 케이스 74건** (`reference/TestCases-legacy.md`) — 긴급도 판정·핵심 정보 보존·역번역·티켓 변환 입력/기대값. `/v1/refine` 통합 테스트의 시드로 재사용.
3. **C6 오탐 방지 규칙** — 감정 신호가 낮은 입력에는 티켓 변환을 제안하지 않는다(감지 임계 프롬프트에 반영됨). Spec의 "감지 시 제안"과 정합.

## 실측으로 얻은 기술 교훈 (같은 함정을 다시 밟지 말 것)

| # | 교훈 | 실측 근거 |
|---|---|---|
| 1 | **jsdom 테스트는 확장 번들 크래시를 원리적으로 못 잡는다** — `process is not defined`로 콘텐츠 스크립트가 전 페이지 즉사했는데 vitest는 전부 green이었다. `vite.config`에 `define: { 'process.env': {} }` 계열 처리 + **실브라우저 unpacked 로드 스모크를 Day 1부터** | 구 T80, 2026-08-10 |
| 2 | **contentEditable에 `textContent` 대입은 조용히 실패하거나 서식을 파괴한다** — 리치 에디터(Gmail 등)는 Range/`execCommand('insertText')` 기반 삽입 필요. Gmail 답장에서 전체 본문 치환은 인용문을 날린다 | 구 T47/T49 스파이크 |
| 3 | **GitHub PR 페이지는 정적 CSS 선택자가 0매치** (React/Primer 동적 렌더) — DOM 의존 기능은 반드시 실페이지에서 선택자 검증 | 구 T29 QA 실측 |
| 4 | **수신자 DOM 자동 감지는 범용 규칙이 성립하지 않는다** — 사이트마다 마크업이 완전히 다름(Discourse 자체 클래스, Google Groups 난독화). Spec audit 2의 "수동 선택 드롭다운 백업"을 백업이 아니라 **주 경로**로 설계할 것 | 구 T66 스파이크, 2026-08-10 |
| 5 | **LLM 실패·크레딧 소진 폴백** — 사전 준비된 데모 응답으로 폴백하되 **"폴백 중" 표시를 화면에 명시**(실제 결과로 오인 금지). 시연 중 키 장애 대비의 생명줄 | 구 AC-029/AC-041, 프로덕션에서 실동작 확인 |
| 6 | **동일 입력 LLM 캐시** — 같은 입력 재호출을 캐시로 돌려주면 비용 절감 + 데모 재현성 확보. 단, "독립 재현 검증"이 필요할 때 캐시가 방해하므로 바이패스 플래그를 처음부터 | 구 llm_cache |
| 7 | **EU AI Act Art 5(1)(f)** — 직장에서 자연인의 감정을 추론하는 AI는 금지(2025-02 발효, 전세계 매출 7% 과징금). **B2B 마찰 리포트("하소연 감지 횟수"를 리더에게 노출)가 정면으로 걸린다.** Spec대로 v1은 목업 배너만 두는 것이 안전하며, 발표에서 EU 시장을 전제로 말하지 않는다 | 구 PRD Risk, measured 2026-08-04 |
| 8 | **개인 점수·국가 단정 회피는 심사에서도 방어력** — 서술형 태그(필수 9의 G1/G2)와 근거 인용("코멘트 평균 2문장 → 간결 제안")은 구 프로젝트에서 프라이버시 리스크를 크게 줄인 검증된 패턴 |
| 9 | **병렬 작업은 반드시 별도 워크트리/클론** — 같은 디렉토리에서 병렬 브랜치 작업 시 HEAD가 서로 바뀜 | 구 T47/T49에서 실제 발생 |
| 10 | **Vercel/호스팅 별칭은 배포 성공과 별개** — 프로덕션 도메인이 옛 배포를 가리키는 사고 2회. 배포 후 반드시 공개 URL을 curl로 실측 | 구 T36, 2026-08-12 |
