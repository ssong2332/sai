/**
 * S18 — B2B 웹 대시보드 목업 데이터.
 *
 * 🔴 전부 합성 데이터다 — 9일 해커톤 특성상 축적된 실데이터가 없어 v1은 목업으로 대체된다
 *    (Spec §3: "9일 해커톤 특성상 축적된 실데이터가 없어 B2B 웹 대시보드(v2)로 합쳐져 출시").
 * 🔴 전부 **팀/조직 단위 집계**다 — 특정 개인을 지목하는 값은 하나도 없다. 이름·이메일·계정
 *    식별자를 담는 필드 자체가 이 파일에 없다(필수 9 G1/G2와 같은 원칙 — 서열·낙인을 유발하는
 *    개인 단위 숫자를 만들지 않는다).
 * 🔴 EU AI Act Art 5(1)(f) 리스크(Lessons #7) — "누가 하소연했는지"가 아니라 "하소연으로 감지된
 *    메시지가 몇 건이었는지"만 센다. 자연인의 감정을 특정 개인에게 귀속시켜 추론하지 않는다.
 * 🔴 2026-08-13 — 클로드 디자인 시안(`Sai Dashboard.dc.html`)의 값을 그대로 옮겼다. 시나리오가
 *    "이번 주"/"지난달" 두 개인 것도 시안 원본 구조를 그대로 따른 것 — 기간에 따라 마찰/긍정
 *    총량이 달라지는 것을 보여주기 위한 데모 장치다.
 */

/**
 * 🔴 **목업은 실제로 만들 수 있는 지표만 보여준다** (2026-08-15 사용자 결정).
 *
 * 「제때 회신함」(`ontime`)을 뺐다. 상대 메시지가 **언제 도착했는지**를 우리가 모르므로 실데이터
 * 쪽에는 이 지표가 없는데(`src/lib/friction.js` 헤더), 데모에만 남겨 두면 **데모가 실제 제품보다
 * 많은 것을 약속한다.** 피치에서 이 화면을 띄우면 없는 기능을 보여주게 된다 — S33·S45에서
 * 반복해 걷어낸 실패와 같은 종류다. 지표 목록은 `dashboard/src/liveData.js`와 **같아야 한다.**
 *
 * 🔴 **`clear`는 `misread`를 넘을 수 없다.** `misread`는 교정 때 오해 소지를 **감지한** 횟수,
 *    `clear`는 그 문장을 **실제로 적용해 내보낸** 횟수라, 「다시 만들기」로 여러 번 교정하면
 *    misread만 늘고 clear는 한 번이다. 예전 값은 이 관계가 거꾸로였다(주간 clear 9 > misread 6,
 *    월간 96 > 88) — 실데이터에서는 나올 수 없는 조합이라 함께 바로잡았다.
 */
export const SCENARIOS = {
  week: {
    label: '이번 주',
    // 🔴 건강도 지수의 **분모** — 다듬은 메시지 총수(Spec §3 「정규화된 마찰」).
    //    긍정 신호 합계가 아니다. `clear`는 정의상 `misread`를 넘을 수 없어 분모가 될 수 없다.
    total: 84,
    positive: [
      { id: 'clear', label: '명확한 요청으로 교정됨', desc: '오해 소지 표현이 교정 과정에서 정리됨', count: 5 },
      { id: 'schedule', label: '예약 제안을 수락함', desc: '오프타임 전송 대신 예약을 선택', count: 4 },
    ],
    friction: [
      { id: 'misread', label: '오해 소지 표현 발생', desc: '교정 전 원문 기준, 교정 후 반영됨', count: 6 },
      { id: 'venting', label: '하소연으로 감지된 메시지', desc: '반복된 불만·피로감 표현 — 메시지 단위 집계', count: 2 },
      { id: 'forceOffHours', label: '오프타임 강행 시도', desc: '긴급도를 올려 예약 제한을 우회', count: 1 },
    ],
  },
  month: {
    label: '지난달',
    total: 612,
    positive: [
      { id: 'clear', label: '명확한 요청으로 교정됨', desc: '오해 소지 표현이 교정 과정에서 정리됨', count: 71 },
      { id: 'schedule', label: '예약 제안을 수락함', desc: '오프타임 전송 대신 예약을 선택', count: 54 },
    ],
    friction: [
      { id: 'misread', label: '오해 소지 표현 발생', desc: '교정 전 원문 기준, 교정 후 반영됨', count: 88 },
      { id: 'venting', label: '하소연으로 감지된 메시지', desc: '반복된 불만·피로감 표현 — 메시지 단위 집계', count: 41 },
      { id: 'forceOffHours', label: '오프타임 강행 시도', desc: '긴급도를 올려 예약 제한을 우회', count: 23 },
    ],
  },
};
