/**
 * 사이드패널 목업 데이터.
 *
 * 🔴 전부 **가짜 데이터**다. S02(Firebase)·S13(프로필/Diff 학습)이 붙으면 이 파일을 통째로
 *    걷어내고 실데이터로 교체한다. 화면 코드가 이 파일 하나만 바라보게 해서 교체 지점을 한
 *    곳으로 모아 뒀다.
 * 🔴 용어집(Spec 필수 7)은 S12에서 `src/lib/glossary.js`(chrome.storage.local 기반 실제 CRUD)로
 *    옮겨졌다 — 더 이상 이 파일의 정적 배열이 아니다.
 * 🔴 인물·회사는 전부 합성이며 실재하지 않는다.
 * 🔴 값은 [DS] 프로토타입 `Sai Prototype.dc.html`의 것을 그대로 옮겼다.
 */

/**
 * 🔴 **`TODAY_STATS`는 삭제했다** (2026-08-15 사용자 지적).
 *
 * 홈의 「교정 14 · 해독 6 · 예약 발송 3」이 여기 박힌 상수였다. 같은 패널의 보관함은 실제 값인
 * 「예약 발송 0」을 보여줬으므로 **한 화면 안에서 3과 0이 동시에 보였다.** S33에서 지운
 * 「남들이 보는 나의 소통 태그」와 같은 유형의 실패다 — 가짜 데이터가 실데이터처럼 보인다.
 *
 * 실제 카운트는 `src/lib/usage.js`가 센다(정수 3개 + 날짜만 저장, Spec 필수 5).
 */

/** 인앱 재화 (Spec §1 Token Economy). */
export const POINTS = {
  balance: 240,
  detail: '회의 시간 양보 +50P · 1초 피드백 +5P',
  usage: '소통 가이드 상세 열람에 사용할 수 있어요.',
};

/**
 * 남들이 보는 내 소통 태그 (Spec 필수 9).
 * 🔴 숫자 점수 표기 금지(G1/G2) — 서술형 태그만. 국가 단위 단정 서술도 금지.
 */
/**
 * 🔴 **`COMMUNICATION_TAGS`는 삭제했다** (2026-08-14 사용자 지적).
 *
 * 「남들이 보는 나의 소통 태그」 카드가 이 상수 3개를 하드코딩해 보여주고 있었고, 함께 있던
 * 「수정」·「비공개」 버튼에는 **onClick이 아예 없었다** — 눌러도 아무 일이 일어나지 않았다.
 *
 * 진짜로 만들 수 없는 데이터였다: "남들이 나를 태그한 것"이 존재하려면 상대도 사이를 써야 하는데
 * v1은 단일 사용자 제품이다. 없는 것을 있는 것처럼 보이는 쪽이 더 나쁘므로 카드를 제거하고,
 * 수신자 섹션에서 **사실을 그대로** 말하도록 바꿨다(상대가 직접 고치는 기능은 아직 없다는 것).
 * 자기 관찰은 「내 문체」 카드가 실제 관측값으로 대신한다(S30 대안).
 */
